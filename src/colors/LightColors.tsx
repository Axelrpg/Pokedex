import { StyleSheet } from "react-native";

export const lightColors = StyleSheet.create({
    primaryBackground: {
        color: '#FFFFFF', // Fondo claro
    },
    primaryText: {
        color: '#121212', // Texto oscuro para contraste
    },
    rippleColor: {
        color: 'rgba(0, 0, 0, .32)', // Color del efecto ripple en modo claro
    },
});
